<Page
    x:Class="ClassManager.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClassManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Image x:Name="user_image" HorizontalAlignment="Left" Height="100" Margin="10,43,0,0" VerticalAlignment="Top" Width="100"/>
        <TextBlock x:Name="user_name" HorizontalAlignment="Left" Margin="110,43,0,0" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Top" Height="48" Width="172" FontSize="26.667"/>
        <AppBarButton Icon="ShowBcc" IsCompact="True" VerticalAlignment="Center" Margin="282,43,0,549">
            <AppBarButton.Flyout>
                <MenuFlyout>
                    <MenuFlyoutItem Text="Edit"/>
                    <MenuFlyoutItem Text="Delete" />
                    <MenuFlyoutItem Text="share" />
                </MenuFlyout>
            </AppBarButton.Flyout>
        </AppBarButton>
    </Grid>
</Page>
